# Installationsskripte

**una-pam-login** - Installiert das PAM-Modul auf dem Ubuntu-Client. Damit die Anmeldung funktioniert muss das Verzeichnis `/opt/una-master` mit der unveränderliche Master-Vorlage des Home-Verzeichnisses `/home/una-user` angelegt sein!
